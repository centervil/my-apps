# Tasks: Playwright Browser Installation Setup

## Implementation
- [ ] `apps/ui-automations/spotify-automation/README.md` を更新し、`pnpm exec playwright install` の手順を追加する。
- [ ] `.github/workflows/ci.yml` を編集し、Playwrightブラウザのインストールステップを追加する。

## Verification
- [ ] ローカル環境で `pnpm exec playwright test` が（ブラウザが見つからないエラーなく）実行できることを確認する。
- [ ] 変更をコミットし、PRを作成してCIが通過することを確認する。
