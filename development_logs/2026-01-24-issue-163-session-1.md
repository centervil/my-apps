# 2026-01-24 Issue 163 Session 1

## 作業内容
- **E2Eテスト構成の修正**: `apps/ui-automations/spotify-automation/playwright.config.ts` を修正し、`firefox` プロジェクト設定から `testIgnore` プロパティを削除しました。これにより、グローバル設定で定義された `testIgnore`（`**/tests/unit/**` を含む）が正しく継承されるようになりました。
- **検証**: `pnpm nx e2e spotify-automation` を実行し、実行されるテスト数が 21件から 18件に減少したことを確認しました。除外されたのは `tests/unit` 以下のユニットテストであり、要件通りです。

## 決定事項
- プロジェクト固有の `testIgnore` がグローバル設定をオーバーライドしてしまう問題を解決するため、プロジェクト固有設定を削除するアプローチを採用しました。

## 課題
- 特になし。
